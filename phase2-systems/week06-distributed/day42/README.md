# Day 42: 분산 KV 스토어 Day2 — Raft 리더 선출 통합

## 🎯 오늘의 목표

- Day 41 KV에 **Raft 리더 선출**을 연동하고, 리더만 쓰기를 받도록 한다.

---

## ✅ 오늘 할 일 (순서대로)

### 1. Raft 리더 선출 연동

- **할 일**: `day42/` 안에 Day 40 Raft 리더 선출 로직을 가져오거나, **현재 리더**를 반환하는 함수/변수를 둔다. KV 클라이언트(또는 상위 로직)는 **쓰기 요청 시 현재 리더에게만** 보내도록 한다. 리더가 바뀌면(시뮬레이션으로 리더 변경) 새 리더에게 요청이 가도록 정리한다.
- **완료 조건**: "리더 선출 결과 + KV 쓰기는 리더로"가 한 코드베이스에 반영되어 있다.

### 2. 단순 장애 시나리오 테스트

- **할 일**: 리더 1개를 의도적으로 중지(또는 플래그로 비활성)한 뒤, 재선출 또는 새 리더로 쓰기 요청이 가는지 확인한다. (단순화: 수동으로 리더를 바꿔도 됨.)
- **완료 조건**: 리더 변경 후 쓰기가 새 리더로 가는 흐름을 한 번 확인했다.

### 3. Anki 카드 추가

- **할 일**: "분산 KV + Raft: 리더만 쓰기, 리더 장애 시 재선출" **카드 1장** 추가.
- **완료 조건**: Phase 2 덱에 반영했다.

---

## 📌 참고 (복습할 개념)

- 쓰기 → 리더, 리더 장애 → 재선출.

---

## ✅ 체크리스트 (할 일 완료 후 표시)

- [ ] **연동**: Raft 리더 + KV 쓰기 라우팅이 구현됨.
- [ ] **테스트**: 리더 변경(또는 장애) 시나리오를 한 번 실행함.
- [ ] **Anki**: 분산 KV + Raft 카드 추가함.
