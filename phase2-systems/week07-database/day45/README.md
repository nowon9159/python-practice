# Day 45: B-Tree 인덱스 — 직접 구현해보기

## 🎯 오늘의 목표

- **B-Tree** 구조를 이해하고, **insert**와 **search**를 지원하는 단순화 구현을 작성한다.

---

## ✅ 오늘 할 일 (순서대로)

### 1. B-Tree 검색/삽입 흐름 정리

- **할 일**: `day45/` 안에 `btree_notes.md`를 만든다. 노드에 여러 키·자식 포인터가 있고, **search**는 루트에서 시작해 키 비교하며 내려가는 것, **insert**는 leaf에 넣고 넘치면 split하는 흐름을 3~5문장으로 적는다. (선택) MySQL InnoDB 문서 참고.
- **완료 조건**: 검색·삽입 흐름을 말로 설명할 수 있다.

### 2. B-Tree 단순화 구현

- **할 일**: `day45/` 안에 `btree.py`를 만든다. **B-Tree 또는 B+ Tree**를 단순화해(차수 작게, 예: 2~3) **insert(key)**, **search(key) -> bool** 을 구현한다. 노드 = 키 리스트 + 자식 노드 리스트(또는 링크). split 로직은 최소한 한 번만 구현해도 됨.
- **완료 조건**: insert 여러 번 후 search로 존재 여부가 맞게 나온다.

### 3. Anki 카드 추가

- **할 일**: "B-Tree: 검색/삽입, 인덱스 용도" **카드 1장** 추가.
- **완료 조건**: Phase 2 덱에 반영했다.

---

## 📌 참고

- 노드에 여러 키·자식. leaf 노드. InnoDB 인덱스 연결.

---

## ✅ 체크리스트

- [ ] **흐름 정리**: 검색·삽입을 문서에 적었다.
- [ ] **구현**: insert/search가 동작하는 B-Tree 코드를 작성했다.
- [ ] **Anki**: B-Tree 카드 추가함.
