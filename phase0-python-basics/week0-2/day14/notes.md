- dict 에서 key 만 뽑거나 value만 뽑거나 하는 걸 생각을 못했음.
    - dict.keys()
    - dict.items()
    - dict.get()
    - 이 함수들을 잘 사용해야 겠다는 생각이 들었다.
    - 꼭 이 함수들 아니더라도 일반 for문에서 dict 넣고 돌리면 key 만 나온다는 사실도 알음
- list_count_return 함수를 만들 때 구분자가 다르면 어떡하지 라는 생각이 들었음. 의도하던 대로 구현이 잘 됐는지 모르겠음