# Day 1 제출 · 검토용 체크리스트

> **사용법**: 할 일을 다 한 뒤 아래를 채우고, AI에게 **"Day 1 제출 검토해줘"** 라고 요청하면 됩니다.

---

## 1. 체크리스트 (본인 완료 표시)

- [ ] **uv**: `uv --version` 확인했고, `uv venv` 후 `uv run python --version`이 동작한다.
- [ ] **hello.py**: `print("Hello, Python")`이 들어 있고, 실행 시 문구가 출력된다.

---

## 2. 검토용 정보 (AI가 확인할 때 사용)

아래 칸에 **실행 결과를 그대로 붙여넣기**하면 검토가 쉽습니다. (선택이지만 권장)

### 2.1 uv 확인

```bash
uv --version
```

**실행 결과** (여기에 붙여넣기):

```

```

### 2.2 Python 버전 확인

```bash
uv run python --version
```

**실행 결과** (여기에 붙여넣기):

```

```

### 2.3 hello.py 실행

```bash
uv run python hello.py
```

**실행 결과** (여기에 붙여넣기):

```

```

### 2.4 제출한 파일

- [ ] `hello.py` — 첫 번째 Python 스크립트 (print + 주석)

---

## 3. 한 줄 회고 (선택)

> 오늘 배운 것 중 가장 기억에 남는 것 한 줄:

```

```
